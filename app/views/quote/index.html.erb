<%= render 'which' %>
<%= render 'book' %>
<%= render 'which' %>
<%= render 'essay' %>
<br>
<%= render 'which' %>
<%= render 'translation' %>
 
 
<script>
 $('document').ready(()=>{
  $('.header-title').on('mouseenter', (event)=>{
   $('.header-description').show();
   $(this).on('mouseleave', ()=>{
    $('.header-description').hide();
   })
  })
  $('.go-menu').on('click', (event) => {
      $('.link-list').toggle();
  })
  $('.checkbox-parts').on('click', (event) => {
   $('#hide').toggle();
  })
  $('.copy-source').on("click",function(){
 $("textarea").select();
 document.execCommand('copy');
 alert("コピー完了");
});
 })
</script>
<%= form_with(model: proofread, local: true) do |form| %>
  <% if proofread.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proofread.errors.count, "error") %> prohibited this proofread from being saved:</h2>

      <ul>
      <% proofread.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="field">
    <%= form.label :edited %>
    <%= form.check_box :edited %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
